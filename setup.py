from setuptools import setup

setup(
    install_requires=[
          "qtile-extras",
          "psutils",
          "dbus-next",
          "pywctl",
      ],
)
python_requires=">=3.11"


